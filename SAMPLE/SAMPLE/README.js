/*Tests-----Launch your browser and open the following URLhttp://127.0.0.1:8081/testServer?path=tests/customLogin.js */ //Examplesvar modulesFolder = FileSystemSync('Modules');var customLogin = require(modulesFolder.path + 'customLogin');//to remove all userscustomLogin.clear();//to add a usercustomLogin.createUser('miyako', 'Keisuke Miyako', 'Admin', 'password');//you should be able to login with the credentials above//debugger;//to loginloginByPassword('miyako', 'password');currentUser();logout();//does not return the entity for security, only a flat objectcustomLogin.getUser('miyako');customLogin.getUsers();